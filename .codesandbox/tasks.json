{
  "setupTasks": [
    "npm install",
    "npm run build:sandbox"
  ],
  "tasks": {
    "app": {
      "name": "Application",
      "command": "npx -y serve -s dist -l 3000 --no-clipboard",
      "runAtStart": true,
      "preview": {
        "port": 3000,
        "readyOn": 200
      }
    }
  }
}
